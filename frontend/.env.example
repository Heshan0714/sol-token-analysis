# Point this at your deployed backend URL (e.g. Railway) before building
# for production. Locally it falls back to http://localhost:4000 if unset.
VITE_API_BASE=https://your-backend-url.up.railway.app
